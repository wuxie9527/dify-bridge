# Dify Bridge 日志轮转配置
# 复制到服务器：/etc/logrotate.d/dify-bridge

/root/dify-bridge/logs/*.log {
    daily
    rotate 7
    size 10M
    compress
    delaycompress
    missingok
    notifempty
    create 0644 root root
}
